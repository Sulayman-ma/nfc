import { View, Text } from 'react-native'
import React from 'react'
import { FIREBASE_AUTH } from '../FirebaseConfig'

const useSignIn = ({setLoading}) => {
 const auth = FIREBASE_AUTH

}

export default useSignIn